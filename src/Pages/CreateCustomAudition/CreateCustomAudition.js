import React from 'react';
import { View, Text } from 'react-native';

/**
 * @function CreateCustomAudition
 * @description Creates a custom audition to be stored and displayed in the app
 * @author Alexander Burdiss
 * @since Not Yet.
 * @version 1.0.0
 * @component
 * @example
 * ```jsx
 * <CreateCustomAudition />
 * ```
 */
const CreateCustomAudition = () => {
  return (
    <View>
      <Text>Hello, Create Custom Audition!</Text>
    </View>
  );
};

export default CreateCustomAudition;
